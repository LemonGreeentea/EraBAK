@CHARACTER_EDIT_UI_NAME(대상)
#DIM 대상
PRINTFORML 성 -- %CSTR:대상:0, MAX_CHARANAME_LENGTH, LEFT% (%CSTR:대상:5, MAX_CHARANAME_LENGTH, LEFT%)
PRINTBUTTON "[입력]", 0
PRINTBUTTON " [랜덤]", 1
PRINTBUTTON " [소거]", 2
PRINTL 
PRINTFORML 명 -- %CSTR:대상:1, MAX_CHARANAME_LENGTH, LEFT% (%CSTR:대상:6, MAX_CHARANAME_LENGTH, LEFT%)
PRINTBUTTON "[입력]", 100
PRINTBUTTON " [랜덤]", 101
PRINTBUTTON " [소거]", 102
PRINTL 
PRINTFORML 자 -- %CSTR:대상:2, MAX_CHARANAME_LENGTH, LEFT% (%CSTR:대상:7, MAX_CHARANAME_LENGTH, LEFT%)
PRINTBUTTON "[입력]", 200
PRINTBUTTON " [랜덤]", 201
PRINTBUTTON " [소거]", 202
PRINTL 

PRINTFORML 애칭 -- %CSTR:대상:98, MAX_CHARANAME_LENGTH, LEFT%
PRINTBUTTON "[입력]", 300
PRINTBUTTON " [소거]", 302
PRINTL
CALL PRINT_TOGGLE_BUTTON("[성명역전]", 999, TALENT:대상:성명역전)
PRINTL

@CHARACTER_EDIT_INPUT_NAME(대상, 입력치)
#DIM 대상
#DIM 입력치
#DIMS 이름
#DIMS 루비

;손입력
IF 입력치 % 100  == 0
	IF 입력치 / 100 != 3
		PRINTFORML 문자열을 입력
		INPUTS
		이름 = %RESULTS%
		PRINTFORML 루비를 입력
		INPUTS
		루비 = %RESULTS%
		CSTR:대상:(입력치 / 100) = %이름%
		CSTR:대상:(입력치 / 100 + 5) = %루비%
		IF 입력치 / 100 == 1
			NAME:대상 = %이름%
			CALLNAME:대상 = %이름%
		ENDIF
	ELSE
		PRINTFORML 문자열을 입력
		INPUTS
		CSTR:대상:98 = %RESULTS%
	ENDIF
;소거
ELSEIF 입력치 % 100 == 2
	IF 입력치 / 100 != 3
		CSTR:대상:(입력치 / 100) = 
		CSTR:대상:(입력치 / 100 + 5) = 
	ELSE
		CSTR:대상:98 = 
	ENDIF
;성 랜덤
ELSEIF 입력치 == 1
;	CALL ASK_MULTI("일본식", "서양식", "중화식")
	CALL ASK_MULTI("일본식", "서양식", "한국식")
	SELECTCASE RESULT
		CASE 0
			CALL GENERATE_FAMILY_NAME_J
		CASE 1
			CALL GENERATE_FAMILY_NAME_K
		CASE 2
;			CALL GENERATE_FAMILY_NAME
			CALL GENERATE_FAMILY_NAME_KR
	ENDSELECT
	CSTR:대상:0 = %RESULTS:0%
	CSTR:대상:5 = %RESULTS:1%
;명 랜덤
ELSEIF 입력치 == 101
;	CALL ASK_MULTI("일본식", "서양식", "중화식")
	CALL ASK_MULTI("일본식", "서양식", "한국식")
	LOCAL = RESULT
;	IF GROUPMATCH(LOCAL, 0, 1)
		CALL ASK_MULTI("남성", "여성")
		LOCAL:1 = RESULT
;	ENDIF
	SELECTCASE LOCAL
		CASE 0
			IF LOCAL:1 == 0
				CALL GENERATE_FIRST_NAME_JM
			ELSE
				CALL GENERATE_FIRST_NAME_JF
			ENDIF
		CASE 1
			IF LOCAL:1 == 0
				CALL GENERATE_FIRST_NAME_KM
			ELSE
				CALL GENERATE_FIRST_NAME_KF
			ENDIF
;		CASE 2
;			CALL GENERATE_FIRST_NAME
		CASE 2
			IF LOCAL:1 == 0
				CALL GENERATE_FIRST_NAME_KRM
			ELSE
				CALL GENERATE_FIRST_NAME_KRF
			ENDIF
	ENDSELECT
	CSTR:대상:1 = %RESULTS:0%
	NAME:대상 = %RESULTS:0%
	CALLNAME:대상 = %RESULTS:0%
	CSTR:대상:6 = %RESULTS:1%
;자 랜덤
ELSEIF 입력치 == 201
	CALL GENERATE_MIDDLE_NAME
	CSTR:대상:2 = %RESULTS:0%
	CSTR:대상:7 = %RESULTS:1%
ELSEIF 입력치 == 999
	TALENT:대상:성명역전 = !TALENT:대상:성명역전
ENDIF
