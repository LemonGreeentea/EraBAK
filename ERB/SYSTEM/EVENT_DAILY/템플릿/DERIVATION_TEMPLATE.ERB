;---------------------
;대응하는 데일리의 DISABLE를 돌려준다. 설정하지 않는 경우, 이벤트는 발생하지 않는다.
;---------------------
@EVENT_DAILY_DERIVATION_XX_DISABLE()
RETURN DAILY_GET_DISABLE_CONFIG("")


;---------------------
;발생 판정
;0기 이후가 되면 발생한다든가, 데일리측에서 이용하고 있는 변수가-1이라면 일으키지 않는다  같은 튕기는 방법을 할 경우에 사용한다
;---------------------
@EVENT_DAILY_DERIVATION_XX_DECISION()
RETURN 1

;---------------------
;특정의 조건을 채우는 캐릭터를 랜덤에 선택하는 경우에 이용
;다른 함수는 필수이지만, 이것만은 없어도 좋다  라고 할까 퍼포먼스에 영향을 주므로 불필요하면 만들어서는 안 된다
;대상이 존재하지 않고 데일리를 개시 할 수 없는 경우는 0을 돌려주는 것으로 데일리의 발생을 캔슬한다
;---------------------
@EVENT_DAILY_DERIVATION_XX_SETTARGET()
FOR LOCAL, 0, CHARANUM

NEXT
RETURN 1

;---------------------
;이벤트의 진행 정도에 응해 이벤트명을 변경하고 싶은 경우에 이용
;변경할 필요가 없는 경우, 이 함수는 불요
;---------------------
@EVENT_DAILY_DERIVATION_XX_NAME()
RESULTS = ""

;---------------------
;본체
;---------------------
@EVENT_DAILY_DERIVATION_XX()

RETURN 1
