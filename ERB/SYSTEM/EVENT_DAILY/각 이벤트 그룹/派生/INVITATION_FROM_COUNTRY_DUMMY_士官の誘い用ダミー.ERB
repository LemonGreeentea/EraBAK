;---------------------
;대응하는 데일리의 DISABLE를 돌려준다. 설정하지 않는 경우, 이벤트는 발생하지 않는다.
;---------------------
@EVENT_DAILY_DERIVATION_INVITATION_FROM_COUNTRY_DUMMY_DISABLE()
RETURN DAILY_GET_DISABLE_CONFIG("INVITATION_FROM_COUNTRY")

;---------------------
;발생 판정
;0기 이후가 되면 발생한다든가, 데일리측에서 이용하고 있는 변수가-1이라면 일으키지 않는다  같은 튕기는 방법을 할 경우에 사용한다
;대응하는 데일리의 DISABLE 체크를 규약으로서 필수로 한다
;---------------------
@EVENT_DAILY_DERIVATION_INVITATION_FROM_COUNTRY_DUMMY_DECISION()
SIF DVAR:관직에오름의권유_비권유남은턴 > 0
	DVAR:관직에오름의권유_비권유남은턴 --
RETURN 0

;---------------------
;본체 여기에는 오지 않지만 안전을 위해서(때문에)
;---------------------
@EVENT_DAILY_DERIVATION_INVITATION_FROM_COUNTRY_DUMMY()

RETURN 1

@EVENT_DAILY_INVITATION_FROM_COUNTRY_DUMMY_ALLOW_WHEN_WANDERING
RETURN 1

