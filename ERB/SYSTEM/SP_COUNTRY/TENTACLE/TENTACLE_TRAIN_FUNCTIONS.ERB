;-------------------------------------------------
;조교 함수 존재 판정
;-------------------------------------------------
@TENTACLE_DOES_TRAIN()

;-------------------------------------------------
;조교 함락시 메세지
;-------------------------------------------------
@TENTACLE_FALLEN_MSG(대상)
#DIM 대상
IF !GETBIT(TALENT:대상:특수세력함락계, 특수세력_촉수)
	PRINTFORMW 촉수들의 능욕으로, %조사처리(ANAME(대상),"는")% 마음도 몸도 접혀 버렸다
	PRINTFORMW 기회를 가늠해, 촉수들은 %ANAME(대상)%의 육체에 기생했다……
	IF TALENT:대상:낙인
		PRINTFORML %조사처리(ANAME(대상),"가")% %ANAME(MASTER)%전용인 것을 나타내는 낙인을,
		PRINTFORMW 기생한 촉수는 초현실적인 힘으로 써 지웠다
		TALENT:대상:낙인 = 0
	ELSE
		PRINTFORMW 촉수는 %ANAME(대상)%의 신체에 깃들이기 시작했다
	ENDIF
	IF TALENT:(대상):프라이드높음
		PRINTL
		PRINTFORMW %조사처리(ANAME(대상),"는")% 기어다니는 촉수 중(안)에서, 모두를 단념한 것 같은 표정을 띄우고 있다
		PRINTFORMW 신체의 구석구석까지 이형에 더럽혀지고 다한 것으로, %ANAME(대상)%의 프라이드는 완전하게 접혀 버린 것 같다……
		CALL TALENT_LOSE_PRIDE(대상, 1, 1)
	ENDIF
ELSE
	PRINTFORMW 촉수들은 다시 %ANAME(대상)%에 기생해, %PRONOUN(대상)%의 의식에 스스로가 촉수의 모판에 지나지 않는다는 인쇄하여 넣기를 실시했다
	PRINTFORMW 세뇌된 %조사처리(ANAME(대상),"는")%, 촉수를 늘린다는 유일한 목적을 위해서(때문에), 그 신체를 바치기로 했다……
ENDIF

;-------------------------------------------------
;조교 함락시에 피어싱 하거나 하는 경우의 처리
;-------------------------------------------------
@TENTACLE_FALLEN_LEAVE_MARK(대상)
#DIM 대상
LOCAL:1 = GET_COUNTRY_FROM_ID(특수세력_촉수)
CALL COLOR_PRINTW(@"촉수들은, 조속히 %조사처리(ANAME(대상),"를")% 모판으로서 이용하기로 했다……", 칼라_핑크)
FOR LOCAL, 0, 5
	CALL FUCK_SP(대상, "욕망, 봉사, 성교, 성기술, 정애, 배설, Ｖ확장, Ａ확장, Ｃ, Ｖ, Ａ, Ｂ, Ｍ, 펠라티오, 키스, 긴박, 고통쾌락, 마조, Ｖ성교, Ａ성교", "처녀상실, 질내사정, Ａ처녀상실, 키스상실, 장내사정, 구강사정", LOCAL:1, GET_SPERM_ID("촉수"), "촉수", "촉수", "", "윤간")
NEXT
IF IS_FEMALE(대상) && !GETBIT(TALENT:대상:음란계, 소질_음란_모판)
	PRINTFORML %ANAME(대상)%의 자궁에 극세의 촉수가 기어들어, 물약을 주입해 나간다……
	CALL COLOR_PRINTW(@"%조사처리(ANAME(대상),"는")%＜모판＞이 되었다", 칼라_핑크)
	SETBIT TALENT:대상:음란계, 소질_음란_모판
ENDIF