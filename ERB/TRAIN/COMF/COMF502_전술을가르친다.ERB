;전술을 가르친다
; 2023-04-09 번역체 교정 - 레몬녹차
;-------------------------------------------------
;커맨드 명칭
;-------------------------------------------------
@COM_NAME502
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):아이의성장도 <= 2
	RESULTS = 책을 읽는다
ELSE
	RESULTS = 전술을 가르친다
ENDIF

;-------------------------------------------------
;선택 가부 판정
;-------------------------------------------------
@COM_ABLE502
;공통 부분
CALL COM_ABLE_COMMON(502)
SIF RESULT == 0
	RETURN 0
;주인공이 플레이어 때 한정
SIF MPLY:0 != MASTER
	RETURN 0
RETURN 1

;-------------------------------------------------
;메인 처리
;-------------------------------------------------
@COM502
CALL COM_SLG_TRAIN_MAIN_PROCESS(GETNUM(ABL, "지략"), 1)
RETURN 1

;-------------------------------------------------
;지문(전문)
;-------------------------------------------------
@COM_TEXT_BEFORE502
;외에서 논다
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):아이의성장도 <= 2
	PRINTFORMW %조사처리(ANAME(MPLY:0),"는")% %조사처리(ANAME(MTAR:0),"와")% 함께 그림책을 읽었다

;무술을가르친다
ELSE
	PRINTFORMW %조사처리(ANAME(MPLY:0),"는")% %ANAME(MTAR:0)%에게 전술 공부를 시켰다
ENDIF

;--------------------------------------------------------
;지문(파라미터·각인 변동 후)
;--------------------------------------------------------
@COM_TEXT_LAST502

;-------------------------------------------------

;커맨드 구분
;-------------------------------------------------

@COM_AVAILABLE_WHEN502
RETURN 커맨드_육아
