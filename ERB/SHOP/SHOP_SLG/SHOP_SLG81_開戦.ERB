;-------------------------------------------------
;「개전」의 명칭
;-------------------------------------------------
@SHOP_SLG_NAME81
IF DAY >= SLG_PP:1
	RESULTS:0 '= "전투시작"
ELSE
	RESULTS:0 '= "다음턴"
ENDIF

;-------------------------------------------------
;「개전」의 선택 가부
;-------------------------------------------------
@SHOP_SLG_CHECK81
;SIF FLAG:관전 모드 && FLAG:클리어 플래그
;	RETURN 0
;SIF CFLAG:MASTER:소속 == 0 && FLAG:클리어 플래그
;	RETURN 0
RETURN 1

;-------------------------------------------------
;「개전」의 왼쪽 컬럼 메뉴의 입력 처리
;-------------------------------------------------
@SHOP_SLG_EVENTBUY81
RESULT = 1
BEGIN TURNEND

