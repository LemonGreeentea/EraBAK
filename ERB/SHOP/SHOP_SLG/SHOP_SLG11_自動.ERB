;-------------------------------------------------
;「자동 지시 위임」의 명칭
;-------------------------------------------------
@SHOP_SLG_NAME11
IF CONFIG:302 == 0
	RESULTS:0 '= "지시행동"
ELSE
	RESULTS:0 '= "위임행동"
ENDIF

;-------------------------------------------------
;「자동 지시 위임」의 버튼 칼라 지정
;-------------------------------------------------
@SHOP_SLG_NAME_COLOR11
;선택중 칼라와 차이를 붙이기 (위해)때문에 색 바꾸고
;온 진출색·오프 후퇴색
IF CONFIG:302 != 0
	SETCOLOR 0xDF5050
ELSE
	SETCOLOR 0x7099DF
ENDIF

;-------------------------------------------------
;「자동 지시 위임」의 다음 행 설명 지정 유무
;-------------------------------------------------
;@SHOP_SLG_NEXT_LINE_ADD11

;-------------------------------------------------
;「자동 지시 위임」의 다음 행 설명 지정 내용
;-------------------------------------------------
;@SHOP_SLG_NEXT_LINE_ADD_TEXT11

;-------------------------------------------------
;「자동 지시 위임」의 선택 가부
;-------------------------------------------------
@SHOP_SLG_CHECK11
SIF FLAG:클리어플래그
	RETURN 0
SIF FLAG:관전모드
	RETURN 0
SIF CFLAG:MASTER:소속 == 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;「자동 지시 위임」의 오른쪽 컬럼 온 판정
;-------------------------------------------------
;@SHOP_SLG_EVENTBUY_COLUMN_RIGHT_ON_CHECK11

;-------------------------------------------------
;「자동 지시 위임」의 왼쪽 컬럼 메뉴의 입력 처리
;-------------------------------------------------
@SHOP_SLG_EVENTBUY11
CONFIG:302 ++
IF CONFIG:302 >= 2
	CONFIG:302 = 0
ENDIF
RETURN 0

;-------------------------------------------------
;「자동 지시 위임」의 표시 처리
;-------------------------------------------------
;@SHOP_SLG_EVENTBUY_SHOW11

