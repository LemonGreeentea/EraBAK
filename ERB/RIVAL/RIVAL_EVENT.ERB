@RIVAL_FIRST(RIVAL_TARGET)
#DIM RIVAL_TARGET
VARSET LOCAL

IF CONFIG:509 == 1
    CALL SINGLE_DRAWLINE
    PRINTFORML 「아, 안녕하세요. 처음 뵙겠습니다.」
    PRINTFORML %조사처리(ANAME(RIVAL_TARGET),"은")% 라이벌에게 인사를 했다.
    PRINTL
    SETCOLOR 칼라_선택불가
    CALL SINGLE_DRAWLINE
    CALL ADD_RIVAL(RIVAL_TARGET, "관심도", 3)
    RESETCOLOR
ENDIF