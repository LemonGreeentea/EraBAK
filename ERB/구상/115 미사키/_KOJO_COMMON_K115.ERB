;-------------------------------------------------
;공통 부분
;-------------------------------------------------

;=================================================
;●구상의 존재 판정
;=================================================
@KOJO_EXIST_K115

;=================================================
;●개시시
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_TRAIN_START_K115

;=================================================
;●종료시
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_TRAIN_END_K115

;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어측 때)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_TARGET_K115

;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어측 때)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_PLAYER_K115

;=================================================
;●커맨드 실행 후(파라미터의 변동 처리가 끝나고 나서 불려 간다)
;※상황에 관계없이 항상 실행된다. 취급 주의※
;=================================================
@KOJO_COM_AFTER_K115
;[속이 빈 것]상태의 경우, 입이 막히고 있는 경우는 구상을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET, "입장착")
	RETURN 0
ENDIF

;-------------------------------------------------
;첫절정은 재사용할 수 있을 것 같은 것으로 여기에 작성
;-------------------------------------------------
;-------------------------------------------------
;처음Ｃ절정
;-------------------------------------------------
IF NOWEX:Ｃ절정 > 0
	IF CFLAG:220 == 0
		CFLAG:220 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ｖ절정
;-------------------------------------------------
IF NOWEX:Ｖ절정 > 0
	IF CFLAG:221 == 0
		CFLAG:221 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ａ절정
;-------------------------------------------------
IF NOWEX:Ａ절정 > 0
	IF CFLAG:222 == 0
		CFLAG:222 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ｂ절정
;-------------------------------------------------
IF NOWEX:Ｂ절정 > 0
	IF CFLAG:223 == 0
		CFLAG:223 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;처음Ｍ절정
;-------------------------------------------------
IF NOWEX:Ｍ절정 > 0
	IF CFLAG:224 == 0
		CFLAG:224 = 1
		;PRINTFORMW 
	ENDIF
ENDIF

;=================================================
;●단체 엔딩 관계
;=================================================
;
;
;
;
;
;
;
;
;
;
;★여기로부터 먼저는 엔딩이 기술되고 있습니다★
;네타바레를 피하고 싶은 (분)편은 열람하지 않게 주의해 주세요.
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
@KOJO_SINGLE_ENDING_K115
;단체 엔딩
;전도시를 통일했을 때, 호감도 5000이상 또한 전캐릭터중 가장 호감도가 높다고 표시된다
;PRINTFORMW 

;RETURN의 값을 0→1으로 바꾸면, Default의 단체 엔딩 메세지가 스킵 된다
RETURN 0

@KOJO_DEAD_ENDING_K115
;참수 엔딩
;자세력이 멸망 했을 때의 선택지로 「……베어라」를 선택해 주인공이 사망했을 경우에, 호감도 5000이상 또한 전캐릭터중 가장 호감도가 높다고 표시된다
;PRINTFORMW 

;RETURN의 값을 0→1으로 바꾸면, Default의 단체 엔딩 메세지가 스킵 된다
RETURN 0
